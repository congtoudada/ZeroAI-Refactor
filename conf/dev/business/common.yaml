stream:
  output_dir: output/detection/business  # 输出目录
  mot_config: conf/dev/business/mot/mot.yaml  # mot配置
  http_config: conf/dev/service/http/http_helper.yaml  # http配置
  save_video:
    enable: True  # 是否保存视频
  draw_vis:
    enable: True  # 是否可视化

# 更新帧率
update_fps: 100