# 手机（屏幕上方）
INCLUDE:
  - conf/pro/stream/stream_root.yaml

stream:
  cam_id: 8  # 摄像头编号（对应数据库）
  url: rtsp://admin:zwjs1357@192.168.1.118:554/h264/ch1/main/av_stream_phone4  # 取流地址（输入）

output_port: camera8  # 输出端口
