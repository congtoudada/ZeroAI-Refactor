INCLUDE:
  - conf/pro/business/count/count_root.yaml
  - conf/pro/business/count/count2_zone.yaml
input_ports:  # 输入端口
  - yolox-count2
output_ports: []  # 输出端口

count:
  reverse: False  # 默认从上到下为进入，从下到上为离开